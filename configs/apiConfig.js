// configs/apiConfig.js
export default {
  // Set this in Vercel dashboard as an environment variable named DOMAIN_URL.
  // Fallback value below is a placeholder — replace with your service or set Vercel env.
  DOMAIN_URL: process.env.DOMAIN_URL || "https://xnxx-dl.vercel.app/"
};
